# waitloop app

The Waitloop Next.js app: hosted waitlist pages, dashboard, REST API, MCP server, and docs.

See the [root README](../../README.md) for the full picture, or run it locally:

```bash
pnpm install
pnpm dev
```

No database needed — local dev falls back to embedded Postgres (PGlite) under `.data/`.
